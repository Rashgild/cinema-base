CREATE TABLE films (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  name VARCHAR,
  name_original VARCHAR,
  country VARCHAR,
  release DATE,
  time VARCHAR,
  tv_series BOOLEAN,
  photo VARCHAR
);
